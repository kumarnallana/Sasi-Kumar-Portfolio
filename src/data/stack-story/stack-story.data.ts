import { stackLayers, type StackLayer } from "./stack-layers.data";

export const stackStory: {
  title: string;
  line: string;
  layers: StackLayer[];
} = {
  title: "THE STACK",
  line: "Seven layers of verified capability and active technical expansion. From language core to component delivery.",
  layers: stackLayers,
};
